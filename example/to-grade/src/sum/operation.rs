/**
 * Adds two numbers and returns the result.
 * This is the to-grade repository. It's expected
 * to test the behaviour of this method.
 */
pub fn add(left: usize, right: usize) -> usize {
    left + right
}
