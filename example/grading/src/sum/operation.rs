/**
 * Adds two numbers and returns the result.
 * This is the solution repository and it's expected
 * for these methods to work with 100% Test accuracy.
 */
pub fn add(left: usize, right: usize) -> usize {
    left + right
}
